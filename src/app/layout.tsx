import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Abhijith Krishnan - Creative Technologist",
  description:
    "Software Developer | Game Developer | 3D Artist | Music Producer. Creative technologist crafting digital experiences through code, games, 3D art, and sound.",
  keywords:
    "software developer, game developer, 3D artist, music producer, Next.js, React, Unreal Engine, Blender",
  authors: [{ name: "Abhijith Krishnan" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased overflow-x-hidden`}
      >
        {children}
      </body>
    </html>
  );
}
